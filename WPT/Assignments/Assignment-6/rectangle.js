function calculateArea(length, width){
     return length*width;
}
const calculateArea = (length, width)=> length*width;

document.writeln(calculateArea(4,6));
document.writeln(calculateArea(10,20));
